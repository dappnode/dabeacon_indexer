services:
  db:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: dabeacon
      POSTGRES_USER: dabeacon
      POSTGRES_PASSWORD: dabeacon
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
